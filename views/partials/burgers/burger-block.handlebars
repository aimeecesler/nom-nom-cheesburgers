
{{#if devoured}}
<li class="list-group-item">
	{{id}}. {{name}}
	<button type="button" class="devour btn btn-danger" data-id="{{id}}" data-status="{{devoured}}">
		Devour it!
	</button>
	</li>
	{{else}}
	<li class="list-group-item disabled">
	{{id}}. {{name}}
	</li>
{{/if}}

